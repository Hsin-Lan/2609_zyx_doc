# CLAUDE.md

This file provides guidance to agent when working with code in this repository.

# 任务说明
你的任务是结合现代汉语规范，做一份文章的校对。工作范围是文件的第150页之后的部分。《（已压缩）近代宁波商帮人物史料辑存2026.8.31》

# 注意事项
需要注意的点有以下部分：
1. 文字部分
    1. 如果文章中有繁体的部分，请标注。
    2. 如果有漏词或漏字，请标注。
    3. 如果有多余的字或词，请标注。
    4. 如果有不符合现代汉语规范的成语，请标注。
    5. 如果有其它你认为有问题的情况，请标注。
2. 标点符号部分
    - 请参考 `15834-2011-gbt-e-300.txt`（GB/T 15834-2011 标点符号用法国家标准）。

# 任务要求
- 文件夹内有上传的文章，命名规范为页数，如 `1135` 代表第 1135 页的原始文档。
- 按顺序，每次处理一个文档（1135、1136、1137……）。
- 在处理当前文档之前，将当前文档的处理状态更新在 `progress.md` 文件里。
- 每次处理文档之前，跳过已经处理的文档（查阅 `progress.md`）。
- 处理完当前文档后，将检查出的问题存入新文档，命名为 `{页数}-proof`，如 `1135-proof`。
- 校对报告格式：每个问题单独一行，注明问题类型、原文、建议改法。
- 在每次处理完文章的校对后，压缩内存，只保留与当前 instruction 相关的内容和 PDF 中的内容。

# 文档范围
当前目录下待处理文档
